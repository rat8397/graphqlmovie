type movie{
    id:Int!,
    name:String!,
    score:Int!
}
type Query{
    movies:[movie]
    movie(id:Int!):movie
}
type Mutation{
    deleteMovie(id:Int!):Boolean
    addMovie(name:String!,score:Int!):movie
}